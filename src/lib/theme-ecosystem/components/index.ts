/**
 * AWIE V2 - Phase 10: Semantic Component Contracts Barrel (Mandate 1).
 *
 * Exports the immutable semantic component contracts.
 *
 * STRICT CONSTRAINT: This module MUST NOT contain any business logic. It is
 * pure presentation type modeling.
 */

export type {
  Action,
  CtaProps,
  FaqItem,
  FaqProps,
  FeatureGridProps,
  FeatureItem,
  Media,
} from './contracts';
